# 🌍 Мультиязычные сборки Shukabase

Система автоматически собирает 3 варианта приложения для каждой платформы.

## 📦 Варианты

| Код | Язык интерфейса | База знаний | Размер (Примерно) |
|-----|-----------------|-------------|-------------------|
| **en** | Английский | Только EN | ~1.5 GB |
| **ru** | Русский | Только RU | ~1.2 GB |
| **all** | Выбор пользователя | EN + RU | ~2.5 GB |

## ⚙️ Как это работает

При сборке используются переменные окружения:

- `SHUKABASE_LANG`: Определяет дефолтный язык и логику UI.
- `SHUKABASE_DATA_ID`: ID Google Drive файла с соответствующей базой данных (zip).

В коде (`rag_api_server.py` и Rust) эти переменные считываются для настройки скачивания правильного архива при первом запуске.

## 🛠 Локальная сборка

Вы можете использовать `magic_build.bat` для локальной сборки всех вариантов сразу.

```bash
.\magic_build.bat
```
