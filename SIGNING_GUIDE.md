# 🔐 Руководство по настройке подписи кода (Code Signing)

Для того чтобы приложение работало корректно на Windows (без SmartScreen предупреждений) и macOS (без "разработчик не подтвержден"), необходимо настроить цифровую подпись.

Shukabase использует встроенный механизм подписи Tauri.

## 🔑 Шаг 1: Генерация ключей

Если вы еще не сгенерировали ключи, сделайте это локально:

```bash
# Windows (PowerShell)
npm run tauri signer generate -- -w ваш_супер_пароль
```

Система создаст два файла:
1. `src-tauri/keys/private_key.pem` (СЕКРЕТНЫЙ КЛЮЧ)
2. `src-tauri/keys/public_key.pem` (Публичный ключ)

**⚠️ ВАЖНО:** Никогда не добавляйте `private_key` в git! Он должен оставаться только у вас и в секретах GitHub.

## 🚀 Шаг 2: Настройка GitHub Secrets

1. Перейдите в репозиторий на GitHub.
2. Откройте **Settings** -> **Secrets and variables** -> **Actions**.
3. Нажмите **New repository secret**.

Добавьте два секрета:

### Секрет 1: `TAURI_PRIVATE_KEY`
- Откройте файл `src-tauri/keys/private_key.pem` в блокноте.
- Скопируйте ВСЁ содержимое (включая `-----BEGIN PRIVATE KEY...`).
- Вставьте в поле Value.

### Секрет 2: `TAURI_KEY_PASSWORD`
- Впишите пароль, который вы придумали на Шаге 1.

## 🖥 Шаг 3: Проверка

После добавления секретов, следующий push в ветку `main` автоматически запустит сборку и подпись.

## 📝 Замечание для macOS
Для полноценной дистрибуции на macOS также требуется Apple Developer ID. Текущая конфигурация использует самоподписанный сертификат Tauri, что убирает базовые ошибки, но для полной чистоты может потребоваться нотаризация Apple (это платный аккаунт).

Для Windows самоподписанный сертификат Tauri обеспечивает целостность обновлений, но SmartScreen может все еще ругаться первое время, пока приложение не наберет репутацию.
