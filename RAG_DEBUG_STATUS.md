
# 🕵️ РУКОВОДСТВО ПО ОТЛАДКЕ (DIAGNOSTIC GUIDE)

Если ассистент зависает или выдает ошибку, выполните следующие действия:

### 1. Откройте Консоль Разработчика
- Нажмите `Ctrl + Shift + I` (или F12) в окне приложения.
- Перейдите на вкладку **Console**.

### 2. Что искать в консоли?

#### Если используется Google (Gemini)
Ищите сообщения, начинающиеся с `[Google Agent]`:
- `[Google Agent] Step X Response: ...` - Это то, что реально отвечает модель.
- Если видите `No Action/Final Answer detected`, значит модель "болтает", но не совершает действий. Это приводит к циклу.
- Если видите `Empty response from model`, это значит, что API вернул пустоту (возможно, сработал фильтр безопасности).

#### Если используется OpenRouter
Ищите сообщения, начинающиеся с `[OpenRouter]`:
- `OpenRouter Error (4xx/5xx): ...` - Точная причина ошибки от сервера (лимиты, контекст).
- `[OpenRouter] Tool Call: ...` - Пытается ли модель искать?
- `[OpenRouter] Found X results...` - Сколько данных мы отправляем обратно. Мы ограничили это число до 5, чтобы не перегружать память модели.

### 3. Исправление "Вечного раздумья"
Если в консоли вы видите, что агент ходит по кругу (Step 1... Step 10...):
1. Попробуйте переформулировать запрос.
2. Сервер может долго (20+ сек) обрабатывать запрос "Re-ranking". Это нормально для локального запуска, но выглядит как зависание.

### 4. Где логи сервера?
Файл: `rag/logs/rag_api_server.log`.
Если там видите `Starting Re-ranking process...` и тишина 20 секунд - это просто медленная работа CPU, не баг.
